#pragma once
#include <windows.h>

// Prototypy funkcji dla okien dialogowych
INT_PTR CALLBACK AddPatientDialogProc(HWND, UINT, WPARAM, LPARAM);
INT_PTR CALLBACK EditPatientDialogProc(HWND, UINT, WPARAM, LPARAM);

